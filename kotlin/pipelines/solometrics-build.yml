name: $(GitVersion.SemVer)

trigger:
  branches:
    include:
    - master
  paths:
    include:
    - kotlin/*

pool:
  vmImage: 'macOS-latest'

variables:
  System.Debug: true
  DOT_KEYSTORE_FILELOCATION: $(Agent.TempDirectory)/solometrics-keystores.jks
  DOT_KEYSTORE_PASSWORD: $(KeystorePassword)
  DOT_KEYALIAS_PASSWORD: $(KeyAliasPassword)
  DOT_BUILD_VERSIONCODE: $[counter()]
  DOT_BUILD_VERSIONNAME: $(GitVersion.MajorMinorPatch) 


pr: none

stages:
- stage: Build
  jobs:
  - job: Default
    steps:
    - task: UseGitVersion@5
      inputs:
        configFilePath: './kotlin/gitversion.yml'
    - task: replacetokens@3
      inputs:
        targetFiles: 'kotlin/app/build.gradle'
        encoding: 'auto'
        writeBOM: true
        actionOnMissing: 'fail'
        keepToken: false
        tokenPrefix: '#{'
        tokenSuffix: '}#'
    - task: Gradle@2
      displayName: Build and Unit Test
      inputs:
        workingDirectory: 'kotlin_tinkering'
        gradleWrapperFile: 'kotlin_tinkering/gradlew'
        gradleOptions: '-Xmx3072m '
        publishJUnitResults: false
        testResultsFiles: '**/TEST-*.xml'
        tasks: 'build'
    - bash: chmod +x kotlin_tinkering/pipeline/kotlin_integration_test.sh
    - bash: ./kotlin_tinkering/pipeline/kotlin_integration_test.sh
    # KUDOS
    # https://medium.com/genetec-tech/android-ui-testing-in-azure-devops-81bbe7cea9fd
    - bash: | 
        ./kotlin_tinkering/gradlew connectedDebugAndroidTest --stacktrace -p ./kotlin_tinkering
        ./kotlin_tinkering/gradlew --stop 
      displayName: 'Run Instrumented Tests' 
    - task: PublishTestResults@2
      displayName: 'Publish Test Results'
      inputs:
        testResultsFiles: '**/kotlin_tinkering/app/build/**/TEST*.xml'
        failTaskOnFailedTests: true
        testRunTitle: 'Test results'
      condition: always()
  - job: PackageAndPublish
    steps:
    - task: replacetokens@3
      inputs:
        targetFiles: 'kotlin/app/build.gradle'
        encoding: 'auto'
        writeBOM: true
        actionOnMissing: 'fail'
        keepToken: false
        tokenPrefix: '#{'
        tokenSuffix: '}#'
    - task: DownloadSecureFile@1
      inputs:
        secureFile: 'solometrics-keystores.jks'
    - task: Gradle@2
      inputs:
        workingDirectory: 'kotlin'
        gradleWrapperFile: 'kotlin/gradlew'
        gradleOptions: '-Xmx3072m '
        publishJUnitResults: false
        testResultsFiles: '**/TEST-*.xml'
        tasks: 'bundleRelease'
    - task: PublishPipelineArtifact@1
      inputs:
        targetPath: '$(Build.SourcesDirectory)/kotlin/app/build/outputs/bundle'
        artifact: 'bundle'
        publishLocation: 'pipeline'
